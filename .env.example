# ── Supabase ───────────────────────────────────────────────────────────────
# Tableau de bord Supabase → Project Settings → API
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Clé service_role : SERVEUR UNIQUEMENT, jamais exposée au client.
SUPABASE_SERVICE_ROLE_KEY=

# ── Resend (emails transactionnels) ─────────────────────────────────────────
# Tableau de bord Resend → API Keys
RESEND_API_KEY=
# Adresse expéditrice (nécessite un domaine vérifié dans Resend).
# Laisser vide → utilise onboarding@resend.dev (tests uniquement, vers son propre email).
EMAIL_FROM=

# ── Bunny Stream (vidéos — branché plus tard) ────────────────────────────────
BUNNY_STREAM_LIBRARY_ID=
BUNNY_STREAM_API_KEY=
BUNNY_STREAM_CDN_HOSTNAME=

# ── PayPal (paiement — décision 2026-07-01 : compte perso + liens PayPal.Me) ─
# Pseudo PayPal.Me du compte (ex. "jefferson" pour paypal.me/jefferson).
# Compte PERSONNEL : pas d'API ni de webhook — l'enseignant confirme
# manuellement la réception dans /teacher/payments.
PAYPAL_ME_USERNAME=

# ── Cron Vercel (relances d'échéances) ───────────────────────────────────────
# Secret aléatoire (openssl rand -hex 32). Vercel l'envoie automatiquement en
# Authorization: Bearer sur les routes cron quand la variable existe.
CRON_SECRET=

# ── Revolut (DORMANT — remplacé par PayPal ; conservé si compte Business un jour) ─
# Revolut Business API (b2b.revolut.com) — gestion du compte professionnel
REVOLUT_API_KEY=
# Revolut Merchant API (merchant.revolut.com) — encaissement des élèves
# Dashboard Revolut Business → Settings → Merchant API → API Keys
REVOLUT_MERCHANT_API_KEY=
# Secret webhook : Dashboard Revolut Merchant → Webhooks → ton endpoint
REVOLUT_WEBHOOK_SECRET=

# ── Zoom (visio — branché plus tard) ─────────────────────────────────────────
ZOOM_ACCOUNT_ID=
ZOOM_CLIENT_ID=
ZOOM_CLIENT_SECRET=
